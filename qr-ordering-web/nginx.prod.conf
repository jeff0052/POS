server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /qr/ {
    alias /usr/share/nginx/html/;
    try_files $uri $uri/ /qr/index.html;
  }

  location / {
    return 301 /qr/;
  }
}
