# Session 3.2 — Buffet Module Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the full buffet lifecycle — package CRUD, table start, timed session, buffet-specific menu, per-person limit enforcement, and checkout pricing.

**Architecture:** New entities (BuffetPackageEntity, BuffetPackageItemEntity) in v2/catalog. Extend existing TableSessionEntity + order item entities with buffet fields. 4 services + 1 validation method + 3 controllers. Unified /menu endpoint delegates to BuffetMenuService for BUFFET mode.

**Tech Stack:** Java 17, Spring Boot, JPA/Hibernate, Mockito for tests

**Spec:** `docs/superpowers/specs/2026-03-29-session-3.2-buffet-module-design.md`

**Compile:** `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && ./mvnw compile -o -q`

**Test:** `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && ./mvnw test -q`

---

## File Structure

### New files (v2/catalog)

| File | Responsibility |
|---|---|
| `v2/catalog/infrastructure/persistence/entity/BuffetPackageEntity.java` | JPA entity for buffet_packages |
| `v2/catalog/infrastructure/persistence/entity/BuffetPackageItemEntity.java` | JPA entity for buffet_package_items |
| `v2/catalog/infrastructure/persistence/repository/JpaBuffetPackageRepository.java` | Repository for packages |
| `v2/catalog/infrastructure/persistence/repository/JpaBuffetPackageItemRepository.java` | Repository for items |
| `v2/catalog/application/dto/BuffetPackageDto.java` | Package response DTO |
| `v2/catalog/application/dto/BuffetPackageItemDto.java` | Item response DTO |
| `v2/catalog/application/dto/BuffetStatusDto.java` | Buffet session status DTO |
| `v2/catalog/application/dto/BuffetBillDto.java` | Pricing result DTO |
| `v2/catalog/application/dto/BuffetOrderValidationResult.java` | Order validation DTO |
| `v2/catalog/interfaces/rest/request/CreateBuffetPackageRequest.java` | Create request |
| `v2/catalog/interfaces/rest/request/UpdateBuffetPackageRequest.java` | Update request |
| `v2/catalog/interfaces/rest/request/StartBuffetRequest.java` | Start buffet request |
| `v2/catalog/interfaces/rest/request/BindSkuRequest.java` | Bind SKU request |
| `v2/catalog/interfaces/rest/request/UpdateBindingRequest.java` | Update binding request |
| `v2/catalog/application/service/BuffetPackageService.java` | Package CRUD + SKU bindings |
| `v2/catalog/application/service/BuffetSessionService.java` | Buffet lifecycle |
| `v2/catalog/application/service/BuffetMenuService.java` | Buffet menu query |
| `v2/catalog/application/service/BuffetPricingService.java` | Checkout pricing |
| `v2/catalog/interfaces/rest/BuffetPackageV2Controller.java` | Package management endpoints |
| `v2/catalog/interfaces/rest/BuffetSessionV2Controller.java` | Session endpoints |
| `v2/catalog/interfaces/rest/BuffetPricingV2Controller.java` | Pricing endpoint |

### New test files

| File | Tests for |
|---|---|
| `test/.../v2/catalog/application/service/BuffetPackageServiceTest.java` | Package CRUD + bindings |
| `test/.../v2/catalog/application/service/BuffetSessionServiceTest.java` | Start + status + validation |
| `test/.../v2/catalog/application/service/BuffetMenuServiceTest.java` | Menu filtering |
| `test/.../v2/catalog/application/service/BuffetPricingServiceTest.java` | Price calculation |

### New migration

| File | Change |
|---|---|
| `db/migration/v2/V108__alter_table_sessions_buffet_extra.sql` | Add `child_count`, `buffet_overtime_minutes` to table_sessions (V067 missed these) |

### Modified files

| File | Change |
|---|---|
| `v2/order/.../entity/TableSessionEntity.java` | Add buffet fields (7 columns: dining_mode, guest_count exist in V067; child_count, buffet_overtime_minutes via V108) |
| `v2/order/.../entity/ActiveTableOrderItemEntity.java` | Add buffet fields (3 columns: is_buffet_included, buffet_surcharge_cents, buffet_inclusion_type per V074) |
| `v2/order/.../entity/SubmittedOrderItemEntity.java` | Add buffet fields (3 columns: same as active items per V074) |
| `v2/catalog/interfaces/rest/MenuV2Controller.java` | Add packageId param to /menu |
| `v2/catalog/application/service/MenuQueryService.java` | Delegate to BuffetMenuService for BUFFET |
| `auth/security/SecurityConfig.java` | Add buffet/status permitAll |

---

## Task 1: Entities + Repositories

**Files:**
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/infrastructure/persistence/entity/BuffetPackageEntity.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/infrastructure/persistence/entity/BuffetPackageItemEntity.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/infrastructure/persistence/repository/JpaBuffetPackageRepository.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/infrastructure/persistence/repository/JpaBuffetPackageItemRepository.java`
- Modify: `pos-backend/src/main/java/com/developer/pos/v2/order/infrastructure/persistence/entity/TableSessionEntity.java`
- Modify: `pos-backend/src/main/java/com/developer/pos/v2/order/infrastructure/persistence/entity/ActiveTableOrderItemEntity.java`
- Modify: `pos-backend/src/main/java/com/developer/pos/v2/order/infrastructure/persistence/entity/SubmittedOrderItemEntity.java`

- [ ] **Step 1: Create BuffetPackageEntity**

```java
package com.developer.pos.v2.catalog.infrastructure.persistence.entity;

import jakarta.persistence.*;
import java.time.LocalDateTime;

@Entity(name = "V2BuffetPackageEntity")
@Table(name = "buffet_packages")
public class BuffetPackageEntity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "store_id", nullable = false)
    private Long storeId;

    @Column(name = "package_code", nullable = false, length = 64)
    private String packageCode;

    @Column(name = "package_name", nullable = false, length = 255)
    private String packageName;

    @Column(name = "description", columnDefinition = "TEXT")
    private String description;

    @Column(name = "price_cents", nullable = false)
    private long priceCents;

    @Column(name = "child_price_cents")
    private Long childPriceCents;

    @Column(name = "child_age_max")
    private Integer childAgeMax;

    @Column(name = "duration_minutes", nullable = false)
    private int durationMinutes;

    @Column(name = "warning_before_minutes", nullable = false)
    private int warningBeforeMinutes;

    @Column(name = "overtime_fee_per_minute_cents", nullable = false)
    private long overtimeFeePerMinuteCents;

    @Column(name = "overtime_grace_minutes", nullable = false)
    private int overtimeGraceMinutes;

    @Column(name = "max_overtime_minutes", nullable = false)
    private int maxOvertimeMinutes;

    @Column(name = "package_status", nullable = false, length = 32)
    private String packageStatus;

    @Column(name = "applicable_time_slots", columnDefinition = "JSON")
    private String applicableTimeSlots;

    @Column(name = "applicable_days", columnDefinition = "JSON")
    private String applicableDays;

    @Column(name = "sort_order", nullable = false)
    private int sortOrder;

    @Column(name = "image_id")
    private Long imageId;

    @Column(name = "created_at", insertable = false, updatable = false)
    private LocalDateTime createdAt;

    @Column(name = "updated_at", insertable = false, updatable = false)
    private LocalDateTime updatedAt;

    @Column(name = "created_by")
    private Long createdBy;

    @Column(name = "updated_by")
    private Long updatedBy;

    protected BuffetPackageEntity() {}

    public BuffetPackageEntity(Long storeId, String packageCode, String packageName,
                               String description, long priceCents, Long childPriceCents,
                               Integer childAgeMax, int durationMinutes, int warningBeforeMinutes,
                               long overtimeFeePerMinuteCents, int overtimeGraceMinutes,
                               int maxOvertimeMinutes, String packageStatus,
                               String applicableTimeSlots, String applicableDays,
                               int sortOrder, Long imageId, Long createdBy) {
        this.storeId = storeId;
        this.packageCode = packageCode;
        this.packageName = packageName;
        this.description = description;
        this.priceCents = priceCents;
        this.childPriceCents = childPriceCents;
        this.childAgeMax = childAgeMax;
        this.durationMinutes = durationMinutes;
        this.warningBeforeMinutes = warningBeforeMinutes;
        this.overtimeFeePerMinuteCents = overtimeFeePerMinuteCents;
        this.overtimeGraceMinutes = overtimeGraceMinutes;
        this.maxOvertimeMinutes = maxOvertimeMinutes;
        this.packageStatus = packageStatus;
        this.applicableTimeSlots = applicableTimeSlots;
        this.applicableDays = applicableDays;
        this.sortOrder = sortOrder;
        this.imageId = imageId;
        this.createdBy = createdBy;
    }

    public void update(String packageCode, String packageName, String description,
                       long priceCents, Long childPriceCents, Integer childAgeMax,
                       int durationMinutes, int warningBeforeMinutes,
                       long overtimeFeePerMinuteCents, int overtimeGraceMinutes,
                       int maxOvertimeMinutes, String packageStatus,
                       String applicableTimeSlots, String applicableDays,
                       int sortOrder, Long imageId, Long updatedBy) {
        this.packageCode = packageCode;
        this.packageName = packageName;
        this.description = description;
        this.priceCents = priceCents;
        this.childPriceCents = childPriceCents;
        this.childAgeMax = childAgeMax;
        this.durationMinutes = durationMinutes;
        this.warningBeforeMinutes = warningBeforeMinutes;
        this.overtimeFeePerMinuteCents = overtimeFeePerMinuteCents;
        this.overtimeGraceMinutes = overtimeGraceMinutes;
        this.maxOvertimeMinutes = maxOvertimeMinutes;
        this.packageStatus = packageStatus;
        this.applicableTimeSlots = applicableTimeSlots;
        this.applicableDays = applicableDays;
        this.sortOrder = sortOrder;
        this.imageId = imageId;
        this.updatedBy = updatedBy;
    }

    // Getters
    public Long getId() { return id; }
    public Long getStoreId() { return storeId; }
    public String getPackageCode() { return packageCode; }
    public String getPackageName() { return packageName; }
    public String getDescription() { return description; }
    public long getPriceCents() { return priceCents; }
    public Long getChildPriceCents() { return childPriceCents; }
    public Integer getChildAgeMax() { return childAgeMax; }
    public int getDurationMinutes() { return durationMinutes; }
    public int getWarningBeforeMinutes() { return warningBeforeMinutes; }
    public long getOvertimeFeePerMinuteCents() { return overtimeFeePerMinuteCents; }
    public int getOvertimeGraceMinutes() { return overtimeGraceMinutes; }
    public int getMaxOvertimeMinutes() { return maxOvertimeMinutes; }
    public String getPackageStatus() { return packageStatus; }
    public String getApplicableTimeSlots() { return applicableTimeSlots; }
    public String getApplicableDays() { return applicableDays; }
    public int getSortOrder() { return sortOrder; }
    public Long getImageId() { return imageId; }
    public LocalDateTime getCreatedAt() { return createdAt; }
    public LocalDateTime getUpdatedAt() { return updatedAt; }
}
```

- [ ] **Step 2: Create BuffetPackageItemEntity**

```java
package com.developer.pos.v2.catalog.infrastructure.persistence.entity;

import jakarta.persistence.*;
import java.time.LocalDateTime;

@Entity(name = "V2BuffetPackageItemEntity")
@Table(name = "buffet_package_items")
public class BuffetPackageItemEntity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "package_id", nullable = false)
    private Long packageId;

    @Column(name = "sku_id", nullable = false)
    private Long skuId;

    @Column(name = "inclusion_type", nullable = false, length = 32)
    private String inclusionType; // INCLUDED, SURCHARGE, EXCLUDED

    @Column(name = "surcharge_cents", nullable = false)
    private long surchargeCents;

    @Column(name = "max_qty_per_person")
    private Integer maxQtyPerPerson;

    @Column(name = "sort_order", nullable = false)
    private int sortOrder;

    @Column(name = "created_at", insertable = false, updatable = false)
    private LocalDateTime createdAt;

    protected BuffetPackageItemEntity() {}

    public BuffetPackageItemEntity(Long packageId, Long skuId, String inclusionType,
                                   long surchargeCents, Integer maxQtyPerPerson, int sortOrder) {
        this.packageId = packageId;
        this.skuId = skuId;
        this.inclusionType = inclusionType;
        this.surchargeCents = surchargeCents;
        this.maxQtyPerPerson = maxQtyPerPerson;
        this.sortOrder = sortOrder;
    }

    public void update(String inclusionType, long surchargeCents,
                       Integer maxQtyPerPerson, int sortOrder) {
        this.inclusionType = inclusionType;
        this.surchargeCents = surchargeCents;
        this.maxQtyPerPerson = maxQtyPerPerson;
        this.sortOrder = sortOrder;
    }

    public Long getId() { return id; }
    public Long getPackageId() { return packageId; }
    public Long getSkuId() { return skuId; }
    public String getInclusionType() { return inclusionType; }
    public long getSurchargeCents() { return surchargeCents; }
    public Integer getMaxQtyPerPerson() { return maxQtyPerPerson; }
    public int getSortOrder() { return sortOrder; }
}
```

- [ ] **Step 3: Create repositories**

`JpaBuffetPackageRepository.java`:
```java
package com.developer.pos.v2.catalog.infrastructure.persistence.repository;

import com.developer.pos.v2.catalog.infrastructure.persistence.entity.BuffetPackageEntity;
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;
import java.util.Optional;

public interface JpaBuffetPackageRepository extends JpaRepository<BuffetPackageEntity, Long> {
    List<BuffetPackageEntity> findByStoreIdOrderBySortOrderAsc(Long storeId);
    List<BuffetPackageEntity> findByStoreIdAndPackageStatusOrderBySortOrderAsc(Long storeId, String status);
    Optional<BuffetPackageEntity> findByStoreIdAndPackageCode(Long storeId, String packageCode);
}
```

`JpaBuffetPackageItemRepository.java`:
```java
package com.developer.pos.v2.catalog.infrastructure.persistence.repository;

import com.developer.pos.v2.catalog.infrastructure.persistence.entity.BuffetPackageItemEntity;
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;
import java.util.Optional;

public interface JpaBuffetPackageItemRepository extends JpaRepository<BuffetPackageItemEntity, Long> {
    List<BuffetPackageItemEntity> findByPackageIdOrderBySortOrderAsc(Long packageId);
    List<BuffetPackageItemEntity> findByPackageIdAndInclusionTypeNotOrderBySortOrderAsc(Long packageId, String excludedType);
    Optional<BuffetPackageItemEntity> findByPackageIdAndSkuId(Long packageId, Long skuId);
}
```

- [ ] **Step 4: Create migration V108 for missing table_sessions columns**

Create `pos-backend/src/main/resources/db/migration/v2/V108__alter_table_sessions_buffet_extra.sql`:
```sql
-- V108: Add child_count and buffet_overtime_minutes to table_sessions
-- V067 added dining_mode, guest_count, buffet_package_id, buffet_started_at, buffet_ends_at, buffet_status
-- but missed child_count and buffet_overtime_minutes from the V075 archive spec
ALTER TABLE table_sessions
  ADD COLUMN child_count INT NOT NULL DEFAULT 0 AFTER guest_count,
  ADD COLUMN buffet_overtime_minutes INT NOT NULL DEFAULT 0 AFTER buffet_status;
```

- [ ] **Step 5: Extend TableSessionEntity with buffet fields**

Add after `closedAt` field (line 42 in `TableSessionEntity.java`). These map to columns from V067 + V108:

```java
    @Column(name = "dining_mode")
    private String diningMode = "A_LA_CARTE";

    @Column(name = "guest_count")
    private int guestCount = 1;

    @Column(name = "child_count")
    private int childCount;

    @Column(name = "buffet_package_id")
    private Long buffetPackageId;

    @Column(name = "buffet_started_at")
    private OffsetDateTime buffetStartedAt;

    @Column(name = "buffet_ends_at")
    private OffsetDateTime buffetEndsAt;

    @Column(name = "buffet_status", length = 32)
    private String buffetStatus;

    @Column(name = "buffet_overtime_minutes")
    private int buffetOvertimeMinutes;
```

Add getters and setters for all new fields.

- [ ] **Step 6: Extend ActiveTableOrderItemEntity + SubmittedOrderItemEntity**

Add after `lineTotalCents` field in both entities. Maps to V074 DDL columns:

```java
    @Column(name = "is_buffet_included")
    private boolean buffetIncluded;

    @Column(name = "buffet_surcharge_cents")
    private long buffetSurchargeCents;

    @Column(name = "buffet_inclusion_type", length = 32)
    private String buffetInclusionType; // INCLUDED, SURCHARGE, EXCLUDED
```

Add getters and setters for all new fields in both entities.
**Note:** DDL has `buffet_inclusion_type` (not `buffet_package_id`). The order item tracks the inclusion type at order time.

- [ ] **Step 7: Compile**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw compile -o -q`
Expected: BUILD SUCCESS

- [ ] **Step 8: Commit**

```bash
git add -A && git commit -m "feat(buffet): add entities + repos + migration V108 + extend session/order entities with buffet fields"
```

---

## Task 2: DTOs + Request Objects

**Files:**
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/dto/BuffetPackageDto.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/dto/BuffetPackageItemDto.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/dto/BuffetStatusDto.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/dto/BuffetBillDto.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/dto/BuffetOrderValidationResult.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/request/CreateBuffetPackageRequest.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/request/UpdateBuffetPackageRequest.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/request/StartBuffetRequest.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/request/BindSkuRequest.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/request/UpdateBindingRequest.java`

- [ ] **Step 1: Create response DTOs**

`BuffetPackageDto.java`:
```java
package com.developer.pos.v2.catalog.application.dto;

import java.util.List;

public record BuffetPackageDto(
    Long packageId, String packageCode, String packageName, String description,
    long priceCents, Long childPriceCents, Integer childAgeMax,
    int durationMinutes, int warningBeforeMinutes,
    long overtimeFeePerMinuteCents, int overtimeGraceMinutes, int maxOvertimeMinutes,
    String packageStatus, List<String> applicableTimeSlots, List<String> applicableDays,
    int sortOrder, String imageUrl
) {}
```

`BuffetPackageItemDto.java`:
```java
package com.developer.pos.v2.catalog.application.dto;

public record BuffetPackageItemDto(
    Long itemId, Long skuId, String skuCode, String skuName,
    String inclusionType, long surchargeCents,
    Integer maxQtyPerPerson, int sortOrder
) {}
```

`BuffetStatusDto.java`:
```java
package com.developer.pos.v2.catalog.application.dto;

import java.time.OffsetDateTime;

public record BuffetStatusDto(
    Long sessionId, String buffetStatus,
    OffsetDateTime startedAt, OffsetDateTime endsAt,
    long remainingMinutes, long overtimeMinutes, long overtimeFeeCents,
    int guestCount, int childCount,
    String packageName, long packagePriceCents,
    boolean forceCheckout
) {}
```

`BuffetBillDto.java`:
```java
package com.developer.pos.v2.catalog.application.dto;

public record BuffetBillDto(
    int guestCount, int childCount,
    long headFeeCents, long surchargeTotal, long extraTotal,
    long overtimeMinutes, long overtimeFeeCents, long grandTotal,
    String packageName, long packagePriceCents, Long childPriceCents,
    int durationMinutes, int overtimeGraceMinutes, int maxOvertimeMinutes
) {}
```

`BuffetOrderValidationResult.java`:
```java
package com.developer.pos.v2.catalog.application.dto;

import java.util.List;

public record BuffetOrderValidationResult(List<ValidatedItem> items) {
    public record ValidatedItem(
        Long skuId, boolean buffetIncluded, long buffetSurchargeCents,
        Long buffetPackageId, boolean rejected, String rejectReason
    ) {}
}
```

- [ ] **Step 2: Create request DTOs**

`CreateBuffetPackageRequest.java`:
```java
package com.developer.pos.v2.catalog.interfaces.rest.request;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import java.util.List;

public record CreateBuffetPackageRequest(
    @NotBlank String packageCode, @NotBlank String packageName, String description,
    @NotNull Long priceCents, Long childPriceCents, Integer childAgeMax,
    int durationMinutes, int warningBeforeMinutes,
    long overtimeFeePerMinuteCents, int overtimeGraceMinutes, int maxOvertimeMinutes,
    List<String> applicableTimeSlots, List<String> applicableDays,
    int sortOrder, Long imageId
) {}
```

`UpdateBuffetPackageRequest.java` — same fields as Create (copy the record).

`StartBuffetRequest.java`:
```java
package com.developer.pos.v2.catalog.interfaces.rest.request;

import jakarta.validation.constraints.NotNull;

public record StartBuffetRequest(
    @NotNull Long packageId, int guestCount, int childCount
) {}
```

`BindSkuRequest.java`:
```java
package com.developer.pos.v2.catalog.interfaces.rest.request;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;

public record BindSkuRequest(
    @NotNull Long skuId, @NotBlank String inclusionType,
    long surchargeCents, Integer maxQtyPerPerson, int sortOrder
) {}
```

`UpdateBindingRequest.java`:
```java
package com.developer.pos.v2.catalog.interfaces.rest.request;

import jakarta.validation.constraints.NotBlank;

public record UpdateBindingRequest(
    @NotBlank String inclusionType, long surchargeCents,
    Integer maxQtyPerPerson, int sortOrder
) {}
```

- [ ] **Step 3: Compile**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw compile -o -q`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat(buffet): add DTOs and request objects"
```

---

## Task 3: BuffetPackageService + Controller + Tests

**Files:**
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/service/BuffetPackageService.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/BuffetPackageV2Controller.java`
- Create: `pos-backend/src/test/java/com/developer/pos/v2/catalog/application/service/BuffetPackageServiceTest.java`

- [ ] **Step 1: Write BuffetPackageServiceTest**

```java
package com.developer.pos.v2.catalog.application.service;

import com.developer.pos.auth.security.AuthContext;
import com.developer.pos.auth.security.AuthenticatedActor;
import com.developer.pos.v2.catalog.application.dto.BuffetPackageDto;
import com.developer.pos.v2.catalog.application.dto.BuffetPackageItemDto;
import com.developer.pos.v2.catalog.infrastructure.persistence.entity.BuffetPackageEntity;
import com.developer.pos.v2.catalog.infrastructure.persistence.entity.BuffetPackageItemEntity;
import com.developer.pos.v2.catalog.infrastructure.persistence.repository.JpaBuffetPackageItemRepository;
import com.developer.pos.v2.catalog.infrastructure.persistence.repository.JpaBuffetPackageRepository;
import com.developer.pos.v2.store.infrastructure.persistence.entity.StoreEntity;
import com.developer.pos.v2.store.infrastructure.persistence.repository.JpaStoreLookupRepository;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockedStatic;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.security.core.Authentication;
import org.springframework.security.core.context.SecurityContext;
import org.springframework.security.core.context.SecurityContextHolder;

import java.util.List;
import java.util.Optional;
import java.util.Set;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class BuffetPackageServiceTest {

    @Mock JpaBuffetPackageRepository packageRepo;
    @Mock JpaBuffetPackageItemRepository itemRepo;
    @Mock JpaStoreLookupRepository storeRepo;
    @InjectMocks BuffetPackageService service;

    private MockedStatic<SecurityContextHolder> securityMock;

    void setupActor(Long merchantId, Set<String> permissions, Set<Long> storeIds) {
        var actor = new AuthenticatedActor(1L, "test", "T001", "STORE_MANAGER",
                merchantId, 10L, storeIds, permissions);
        SecurityContext ctx = mock(SecurityContext.class);
        Authentication auth = mock(Authentication.class);
        when(auth.getPrincipal()).thenReturn(actor);
        when(ctx.getAuthentication()).thenReturn(auth);
        securityMock = mockStatic(SecurityContextHolder.class);
        securityMock.when(SecurityContextHolder::getContext).thenReturn(ctx);
    }

    @AfterEach
    void tearDown() {
        if (securityMock != null) securityMock.close();
    }

    StoreEntity storeOf(Long storeId, Long merchantId) {
        StoreEntity s = mock(StoreEntity.class);
        when(s.getMerchantId()).thenReturn(merchantId);
        return s;
    }

    CreateBuffetPackageRequest createReq() {
        return new CreateBuffetPackageRequest("PKG_A", "Package A", null,
                16800L, null, null, 90, 10, 0, 5, 60, null, null, 0, null);
    }

    @Test
    void createPackage_success() {
        setupActor(100L, Set.of("BUFFET_MANAGE"), Set.of(10L));
        when(storeRepo.findById(10L)).thenReturn(Optional.of(storeOf(10L, 100L)));
        when(packageRepo.save(any())).thenAnswer(inv -> inv.getArgument(0));
        BuffetPackageDto dto = service.createPackage(10L, createReq());
        assertNotNull(dto);
        assertEquals("PKG_A", dto.packageCode());
    }

    @Test
    void createPackage_noPermission_throws() {
        setupActor(100L, Set.of("MENU_VIEW"), Set.of(10L));
        assertThrows(SecurityException.class, () -> service.createPackage(10L, createReq()));
    }

    @Test
    void createPackage_wrongStore_throws() {
        setupActor(100L, Set.of("BUFFET_MANAGE"), Set.of(20L)); // no access to store 10
        when(storeRepo.findById(10L)).thenReturn(Optional.of(storeOf(10L, 100L)));
        assertThrows(SecurityException.class, () -> service.createPackage(10L, createReq()));
    }

    @Test
    void listPackages_menuViewSufficient() {
        setupActor(100L, Set.of("MENU_VIEW"), Set.of(10L));
        when(storeRepo.findById(10L)).thenReturn(Optional.of(storeOf(10L, 100L)));
        when(packageRepo.findByStoreIdAndPackageStatusOrderBySortOrderAsc(10L, "ACTIVE"))
                .thenReturn(List.of());
        List<BuffetPackageDto> result = service.listPackages(10L);
        assertNotNull(result);
    }

    @Test
    void deletePackage_softDelete() {
        setupActor(100L, Set.of("BUFFET_MANAGE"), Set.of(10L));
        when(storeRepo.findById(10L)).thenReturn(Optional.of(storeOf(10L, 100L)));
        BuffetPackageEntity pkg = new BuffetPackageEntity(10L, "PKG_A", "A", null,
                16800L, null, null, 90, 10, 0, 5, 60,
                "ACTIVE", null, null, 0, null, 1L);
        when(packageRepo.findById(1L)).thenReturn(Optional.of(pkg));
        service.deletePackage(10L, 1L);
        assertEquals("INACTIVE", pkg.getPackageStatus());
    }

    @Test
    void bindSku_success() {
        setupActor(100L, Set.of("BUFFET_MANAGE"), Set.of(10L));
        when(storeRepo.findById(10L)).thenReturn(Optional.of(storeOf(10L, 100L)));
        BuffetPackageEntity pkg = new BuffetPackageEntity(10L, "PKG_A", "A", null,
                16800L, null, null, 90, 10, 0, 5, 60,
                "ACTIVE", null, null, 0, null, 1L);
        when(packageRepo.findById(1L)).thenReturn(Optional.of(pkg));
        when(itemRepo.findByPackageIdAndSkuId(1L, 50L)).thenReturn(Optional.empty());
        when(itemRepo.save(any())).thenAnswer(inv -> inv.getArgument(0));
        BuffetPackageItemDto dto = service.bindSku(10L, 1L, 50L, "INCLUDED", 0, null);
        assertEquals("INCLUDED", dto.inclusionType());
    }

    @Test
    void bindSku_duplicate_throws() {
        setupActor(100L, Set.of("BUFFET_MANAGE"), Set.of(10L));
        when(storeRepo.findById(10L)).thenReturn(Optional.of(storeOf(10L, 100L)));
        BuffetPackageEntity pkg = new BuffetPackageEntity(10L, "PKG_A", "A", null,
                16800L, null, null, 90, 10, 0, 5, 60,
                "ACTIVE", null, null, 0, null, 1L);
        when(packageRepo.findById(1L)).thenReturn(Optional.of(pkg));
        when(itemRepo.findByPackageIdAndSkuId(1L, 50L)).thenReturn(Optional.of(mock(BuffetPackageItemEntity.class)));
        assertThrows(IllegalStateException.class, () ->
                service.bindSku(10L, 1L, 50L, "INCLUDED", 0, null));
    }

    @Test
    void updatePackage_success() {
        setupActor(100L, Set.of("BUFFET_MANAGE"), Set.of(10L));
        when(storeRepo.findById(10L)).thenReturn(Optional.of(storeOf(10L, 100L)));
        BuffetPackageEntity pkg = new BuffetPackageEntity(10L, "PKG_A", "A", null,
                16800L, null, null, 90, 10, 0, 5, 60,
                "ACTIVE", null, null, 0, null, 1L);
        when(packageRepo.findById(1L)).thenReturn(Optional.of(pkg));
        var updateReq = new UpdateBuffetPackageRequest("PKG_B", "B", null,
                19800L, null, null, 120, 15, 100, 5, 60, null, null, 1, null);
        BuffetPackageDto dto = service.updatePackage(10L, 1L, updateReq);
        assertEquals("PKG_B", dto.packageCode());
        assertEquals(19800L, dto.priceCents());
    }
}
```

- [ ] **Step 2: Run tests — expect FAIL (service not created yet)**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw test -pl . -Dtest=BuffetPackageServiceTest -q`
Expected: COMPILATION FAILURE (BuffetPackageService not found)

- [ ] **Step 3: Write BuffetPackageService**

Full service implementation following the pattern from `MenuTimeSlotManagementService`:
- Constructor injection of repos + ObjectMapper
- `enforceBuffetManage()`, `enforceBuffetViewOrManage()`, `enforceStoreAccess(storeId)` helpers
- CRUD methods as specified in spec Section 4.1
- `toDto()` and `toItemDto()` converters
- JSON read/write helpers for applicableTimeSlots and applicableDays

Key implementation details:
- All read methods: `@Transactional(readOnly = true)`
- All write methods: `@Transactional`
- `createPackage(storeId, CreateBuffetPackageRequest)`: enforce BUFFET_MANAGE + store access, save entity, return DTO
- `updatePackage(storeId, packageId, UpdateBuffetPackageRequest)`: find + verify store, call entity.update(), return DTO
- `deletePackage`: find entity, verify store match, set packageStatus="INACTIVE"
- `bindSku`: verify package belongs to store, check no duplicate (packageId+skuId), save
- `listPackages`: enforce view permission, return ACTIVE packages for storeId

- [ ] **Step 4: Write BuffetPackageV2Controller**

Follow `MenuV2Controller` pattern:
```java
@RestController
@RequestMapping("/api/v2/stores/{storeId}/buffet-packages")
public class BuffetPackageV2Controller implements V2Api {
    // 9 endpoints as per spec Section 5.1
}
```

- [ ] **Step 5: Run tests — expect PASS**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw test -pl . -Dtest=BuffetPackageServiceTest -q`
Expected: 8 tests PASS

- [ ] **Step 6: Compile full project**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw compile -o -q`
Expected: BUILD SUCCESS

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(buffet): add BuffetPackageService + controller + 8 tests"
```

---

## Task 4: BuffetSessionService + Controller + Tests

**Files:**
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/service/BuffetSessionService.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/BuffetSessionV2Controller.java`
- Create: `pos-backend/src/test/java/com/developer/pos/v2/catalog/application/service/BuffetSessionServiceTest.java`

- [ ] **Step 1: Write BuffetSessionServiceTest**

12 test cases covering:

**Start buffet (4 cases):**
- `startBuffet_success` — creates ACTIVE buffet session with correct timestamps
- `startBuffet_packageNotActive_throws400` — package status=INACTIVE → 400
- `startBuffet_alreadyBuffet_throws409` — session.diningMode=BUFFET → 409
- `startBuffet_packageWrongStore_throws404` — package.storeId != storeId → 404

**Get status (3 cases):**
- `getStatus_active` — remainingMinutes > warningBefore → status=ACTIVE
- `getStatus_warning` — 0 < remaining <= warningBefore → status=WARNING
- `getStatus_overtime` — remaining <= 0 → status=OVERTIME, overtimeFee calculated, forceCheckout when exceeds max

**Validate order (5 cases per spec Section 8):**
- `validateOrder_allIncluded_pass` — all items in package as INCLUDED → all pass
- `validateOrder_itemNotInPackage_markedAsExtra` — unknown SKU → isBuffetIncluded=false
- `validateOrder_excludedItem_rejected` — EXCLUDED item → rejected with 400 message
- `validateOrder_limitAtBoundary_pass` — qty exactly at limit → pass
- `validateOrder_limitExceeded_rejected` — alreadyOrdered + qty > limit × guestCount → rejected

Key mock setup:
- Mock `JpaTableSessionRepository.findFirstByStoreIdAndTableIdAndSessionStatusOrderByIdDesc()` for session lookup
- Mock `JpaBuffetPackageRepository.findById()` for package lookup
- Mock `JpaBuffetPackageItemRepository.findByPackageIdAndSkuId()` for validation
- Use `java.time.Clock` or fixed timestamps for deterministic time assertions

- [ ] **Step 2: Write BuffetSessionService**

Dependencies: `JpaTableSessionRepository`, `JpaBuffetPackageRepository`, `JpaBuffetPackageItemRepository`, `JpaStoreLookupRepository`

Key methods:
- `startBuffet(storeId, tableId, request)`:
  1. `enforceBuffetStart()` — check BUFFET_START permission
  2. `enforceStoreAccess(storeId)`
  3. Load package, verify storeId match + ACTIVE
  4. Find open session: `sessionRepo.findFirstByStoreIdAndTableIdAndSessionStatusOrderByIdDesc(storeId, tableId, "OPEN")`
  5. Verify session.diningMode != "BUFFET" (409 if so)
  6. Set buffet fields on session, save
  7. Return `BuffetStatusDto`

- `getBuffetStatus(storeId, tableId)`:
  1. Find open session
  2. Compute from `buffetStartedAt`, `buffetEndsAt`, package fields
  3. `OffsetDateTime now = OffsetDateTime.now()`, `Duration.between(now, endsAt).toMinutes()`
  4. Return computed DTO

- `validateBuffetOrder(sessionId, items)`:
  1. Load session by id, verify BUFFET mode
  2. For each item: lookup packageItem, apply rules, return annotated result

- [ ] **Step 3: Write BuffetSessionV2Controller**

```java
@RestController
@RequestMapping("/api/v2/stores/{storeId}/tables/{tableId}/buffet")
public class BuffetSessionV2Controller implements V2Api {
    // POST /start, GET /status
}
```

- [ ] **Step 4: Add SecurityConfig entry for buffet/status**

In `SecurityConfig.java`, add BEFORE the broad `/stores/**` matcher:
```java
.requestMatchers(HttpMethod.GET, "/api/v2/stores/*/tables/*/buffet/status").permitAll()
```

- [ ] **Step 5: Run tests — expect PASS**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw test -pl . -Dtest=BuffetSessionServiceTest -q`
Expected: 12 tests PASS

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(buffet): add BuffetSessionService + controller + SecurityConfig + 8 tests"
```

---

## Task 5: BuffetMenuService + Unified Menu Integration + Tests

**Files:**
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/service/BuffetMenuService.java`
- Modify: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/MenuV2Controller.java` (line 38-44)
- Modify: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/service/MenuQueryService.java` (queryMenu method)
- Create: `pos-backend/src/test/java/com/developer/pos/v2/catalog/application/service/BuffetMenuServiceTest.java`

- [ ] **Step 1: Write BuffetMenuServiceTest**

5 test cases:
- `getMenu_includesIncludedAndSurcharge_excludesExcluded` — filter by inclusionType
- `getMenu_surchargeAnnotated` — surchargeCents passed through to DTO
- `getMenu_maxQtyPassedThrough` — maxQtyPerPerson in DTO
- `getMenu_groupedByCategory` — products grouped under categories
- `getMenu_imageUrlResolved` — ImageUploadService.resolvePublicUrls called

Key mock setup:
- Mock `JpaBuffetPackageItemRepository.findByPackageIdAndInclusionTypeNotOrderBySortOrderAsc()` to return items excluding EXCLUDED
- Mock `JpaSkuRepository` to return SKUs with product/category info
- Mock `ImageUploadService.resolvePublicUrls()` to return CDN URLs

- [ ] **Step 2: Write BuffetMenuService**

Dependencies: `JpaBuffetPackageItemRepository`, `JpaBuffetPackageRepository`, sku/product/category repos, `ImageUploadService`

```java
@Service
public class BuffetMenuService implements UseCase {
    // getBuffetMenu(storeId, packageId) → List<MenuCategoryDto>
    // 1. Verify package belongs to store + is ACTIVE
    // 2. Load items WHERE inclusionType != 'EXCLUDED'
    // 3. Batch-load SKUs by skuIds
    // 4. Batch-load products by productIds from SKUs
    // 5. Batch-load categories by categoryIds from products
    // 6. Batch-resolve image URLs
    // 7. Group by category, build MenuCategoryDto with buffet-annotated SKUs
}
```

- [ ] **Step 3: Add packageId param to MenuV2Controller.queryMenu**

In `MenuV2Controller.java`, modify the `/menu` endpoint (line 38-44):

```java
@GetMapping("/menu")
public ApiResponse<MenuQueryResultDto> queryMenu(
        @PathVariable Long storeId,
        @RequestParam(required = false) String diningMode,
        @RequestParam(required = false) Long timeSlotId,
        @RequestParam(required = false) Long packageId   // <-- NEW
) {
    return ApiResponse.success(menuQueryService.queryMenu(storeId, diningMode, timeSlotId, packageId));
}
```

- [ ] **Step 4: Add BUFFET delegation to MenuQueryService**

In `MenuQueryService.queryMenu()`, add at the top of the method:

```java
if ("BUFFET".equalsIgnoreCase(diningMode) && packageId != null) {
    return new MenuQueryResultDto(buffetMenuService.getBuffetMenu(storeId, packageId));
}
```

Add a new overloaded method `queryMenu(Long storeId, String diningMode, Long timeSlotId, Long packageId)` that the controller calls. Keep the old 3-param method as a delegate: `return queryMenu(storeId, diningMode, timeSlotId, null)`. This avoids breaking existing callers. Inject `BuffetMenuService` as a dependency.

- [ ] **Step 5: Run tests — expect PASS**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw test -pl . -Dtest=BuffetMenuServiceTest -q`
Expected: 5 tests PASS

- [ ] **Step 6: Compile full project**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw compile -o -q`
Expected: BUILD SUCCESS (verify MenuQueryService still compiles with new param)

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(buffet): add BuffetMenuService + unified /menu delegation + 5 tests"
```

---

## Task 6: BuffetPricingService + Controller + Tests

**Files:**
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/application/service/BuffetPricingService.java`
- Create: `pos-backend/src/main/java/com/developer/pos/v2/catalog/interfaces/rest/BuffetPricingV2Controller.java`
- Create: `pos-backend/src/test/java/com/developer/pos/v2/catalog/application/service/BuffetPricingServiceTest.java`

- [ ] **Step 1: Write BuffetPricingServiceTest**

6 test cases:
- `calculate_adultsOnly_noExtras` — headFee = priceCents × guestCount, grandTotal = headFee
- `calculate_adultsAndChildren` — headFee = adult×guest + child×childCount
- `calculate_withSurchargeItems` — surchargeTotal = sum(qty × buffetSurchargeCents) for included items
- `calculate_withExtraItems` — extraTotal = sum(lineTotalCents) for non-included items
- `calculate_overtimeWithGrace` — overtime = (actualMin - duration - grace) × feePerMin
- `calculate_overtimeCapped` — overtime capped at maxOvertimeMinutes

Mock setup:
- Mock `JpaTableSessionRepository.findById()` for session
- Mock `JpaBuffetPackageRepository.findById()` for package
- Mock `JpaSubmittedOrderRepository.findAllByTableSessionIdOrderByIdAsc()` for orders
- Each order has mocked items with buffet fields

- [ ] **Step 2: Write BuffetPricingService**

Dependencies: `JpaTableSessionRepository`, `JpaBuffetPackageRepository`, `JpaSubmittedOrderRepository`, `JpaStoreLookupRepository`

```java
@Service
public class BuffetPricingService implements UseCase {
    public BuffetBillDto calculateBuffetTotal(Long storeId, Long sessionId) {
        // 1. enforceBuffetStart() + enforceStoreAccess(storeId)
        // 2. Load session by id, verify BUFFET mode
        // 3. Load package by session.buffetPackageId
        // 4. headFee = pkg.priceCents * session.guestCount
        //            + (pkg.childPriceCents ?? 0) * session.childCount
        // 5. Load all submitted orders for session
        // 6. Flatten to items, compute:
        //    surchargeTotal = items.filter(buffetIncluded && surchargeCents>0).sum(qty * surchargeCents)
        //    extraTotal = items.filter(!buffetIncluded).sum(lineTotalCents)
        // 7. Overtime: Duration.between(session.buffetStartedAt, now).toMinutes()
        //    billableOvertime = max(0, actualMin - pkg.durationMinutes - pkg.overtimeGraceMinutes)
        //    billableOvertime = min(billableOvertime, pkg.maxOvertimeMinutes)
        //    overtimeFee = billableOvertime * pkg.overtimeFeePerMinuteCents
        // 8. grandTotal = headFee + surchargeTotal + extraTotal + overtimeFee
        // 9. Return BuffetBillDto
    }
}
```

- [ ] **Step 3: Write BuffetPricingV2Controller**

```java
@RestController
@RequestMapping("/api/v2/stores/{storeId}/buffet-pricing")
public class BuffetPricingV2Controller implements V2Api {
    @GetMapping("/calculate")
    public ApiResponse<BuffetBillDto> calculateBuffetTotal(
            @PathVariable Long storeId,
            @RequestParam Long sessionId
    ) {
        return ApiResponse.success(pricingService.calculateBuffetTotal(storeId, sessionId));
    }
}
```

- [ ] **Step 4: Run tests — expect PASS**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw test -pl . -Dtest=BuffetPricingServiceTest -q`
Expected: 6 tests PASS

- [ ] **Step 5: Run all buffet tests**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw test -q`
Expected: ALL tests PASS (including existing tests)

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(buffet): add BuffetPricingService + controller + 6 tests"
```

---

## Task 7: Final Verification

- [ ] **Step 1: Full compile**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw compile -o -q`
Expected: BUILD SUCCESS

- [ ] **Step 2: Full test suite**

Run: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/ontanetwork/Documents/Codex/pos-backend && ./mvnw test -q`
Expected: ALL tests PASS

- [ ] **Step 3: Verify file count**

Run: `find pos-backend/src -name "*.java" -newer docs/superpowers/specs/2026-03-29-session-3.2-buffet-module-design.md | wc -l`
Expected: ~25 new/modified files

- [ ] **Step 4: Verify line count within budget**

Run: `git diff --stat HEAD~6 -- pos-backend/src/ | tail -1`
Expected: ~1000-1200 lines changed
